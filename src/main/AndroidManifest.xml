<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:installLocation="auto"
    package="zame.game">

    <!-- xlargeScreens - For Android versions 3.2 or higher, there is a Zooming-In button
    which expands apps made for smart phones into a size fit for tablets.
    However, because this function obstructs proper running of ads, it should not be used. -->

    <supports-screens
        android:anyDensity="true"
        android:xlargeScreens="true" />

    <!--
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- android:hardwareAccelerated="false" -->
    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:name=".App"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">

        <activity
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:name=".LauncherActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Gloomy.Theme.LauncherActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:alwaysRetainTaskState="true"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:name=".MainActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Gloomy.Theme.MainActivity" />

        <activity
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:launchMode="standard"
            android:name=".misc.GeneralWebActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Gloomy.Theme.WebActivity" />

        <activity
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:launchMode="standard"
            android:name=".misc.DummyRewardedVideoActivity"
            android:screenOrientation="landscape"
            android:theme="@style/Gloomy.Theme.WebActivity" />

        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />

        <meta-data
            android:name="io.fabric.ApiKey"
            android:value="ecb19f2d73915c06f86059a1048d0eb35664e048" />

    </application>
</manifest>
